import { Behavior } from './Behavior.js';
import { Particle } from '../Particle.js';
import { FunctionValueGenerator, ValueGenerator } from '../functions/index.js';
import { Quaternion, Vector3 } from '../math/index.js';
export declare class OrbitOverLife implements Behavior {
    orbitSpeed: FunctionValueGenerator | ValueGenerator;
    axis: Vector3;
    type: string;
    rotation: Quaternion;
    temp: Vector3;
    constructor(orbitSpeed: FunctionValueGenerator | ValueGenerator, axis?: Vector3);
    initialize(particle: Particle): void;
    update(particle: Particle, delta: number): void;
    frameUpdate(delta: number): void;
    toJSON(): any;
    static fromJSON(json: any): Behavior;
    clone(): Behavior;
    reset(): void;
}
