import { Behavior } from './Behavior.js';
import { Particle } from '../Particle.js';
import { ValueGenerator } from '../functions/ValueGenerator.js';
import { Quaternion, Vector3 } from '../math/index.js';
export declare class ChangeEmitDirection implements Behavior {
    angle: ValueGenerator;
    type: string;
    _temp: Vector3;
    _q: Quaternion;
    memory: {
        data: never[];
        dataCount: number;
    };
    constructor(angle: ValueGenerator);
    initialize(particle: Particle): void;
    update(particle: Particle, delta: number): void;
    frameUpdate(delta: number): void;
    toJSON(): any;
    static fromJSON(json: any): Behavior;
    clone(): Behavior;
    reset(): void;
}
